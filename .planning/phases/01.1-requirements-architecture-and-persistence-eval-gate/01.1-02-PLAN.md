---
phase: 01.1-requirements-architecture-and-persistence-eval-gate
plan: 02
type: execute
wave: 1
depends_on:
  - 01.1-01
files_modified:
  - .planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-DATA-ARCHITECTURE.md
  - .planning/PROJECT.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements:
  - GRID-06
  - MKT-01
  - MKT-02
  - MKT-05
  - HIST-01
  - HIST-02
  - END-01
user_setup: []
must_haves:
  truths:
    - The project chooses one SQLite package and one clear boundary between preferences and structured data.
    - The target table list covers game history, wallet, joker inventory and active-session restore.
    - The future code layout for gameplay and persistence is written before Phase 2 implementation.
    - Adjustable rule constants are config-driven, but gameplay behavior remains code-driven.
    - The runtime config format is frozen as JSON rather than YAML.
  artifacts:
    - .planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-DATA-ARCHITECTURE.md
  key_links:
    - shared_preferences remains limited to profile and lightweight settings.
    - sqflite becomes the canonical structured-data store.
    - assets/config/game_rules.json holds balancing-style constants only.
---

<objective>
Freeze the persistence architecture, SQLite schema and target folder structure so the gameplay phases do not invent data boundaries ad hoc.

Purpose: Decide once where score history, coin state, joker inventory and active-session resume live, how configurable constants are sourced, and how that maps into repositories and folders.
Output: A data-architecture document plus any required planning-level architecture updates.
</objective>

<execution_context>
@/Users/enesdemir/.codex/get-shit-done/workflows/execute-plan.md
@/Users/enesdemir/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-CONTEXT.md
@.planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-RESEARCH.md
@lib/src/core/storage/local_storage_service.dart
@lib/src/core/repositories/profile_repository.dart
@lib/src/core/models/game_result.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical persistence boundary, config boundary and SQLite schema proposal</name>
  <files>.planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-DATA-ARCHITECTURE.md</files>
  <read_first>.planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-RESEARCH.md, .planning/codebase/ARCHITECTURE.md, pubspec.yaml</read_first>
  <action>Write a persistence decision document that names the chosen SQLite package, the supporting package(s), database filename, migration strategy and canonical ownership split between `shared_preferences` and SQLite. In the same document, freeze the config strategy as `config-driven constants` plus `code-driven logic`, choose `JSON` over `YAML`, name the target config file (`assets/config/game_rules.json`), and list exactly which rule categories belong in config versus code. Define the minimum schema needed for `game_results`, `player_wallet`, `joker_inventory` and `session_checkpoint`, and explicitly call out what is intentionally not persisted as a separate table.</action>
  <verify>rg -n "sqflite|shared_preferences|game_results|player_wallet|joker_inventory|session_checkpoint|word_crush.db|game_rules.json|JSON|YAML|config-driven|code-driven" .planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-DATA-ARCHITECTURE.md</verify>
  <acceptance_criteria>
    - one canonical package choice is made for SQLite
    - each required table has a clear responsibility
    - app background/resume state has a documented persistence source
    - config scope is clearly separated from code-only behavior
  </acceptance_criteria>
  <done>The repo now has a written persistence architecture instead of an implied one.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze the future folder layout for gameplay, config and persistence code</name>
  <files>.planning/PROJECT.md, .planning/codebase/ARCHITECTURE.md, .planning/codebase/STRUCTURE.md</files>
  <read_first>.planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-DATA-ARCHITECTURE.md, .planning/codebase/STRUCTURE.md</read_first>
  <action>Update architecture/structure planning docs so future implementation work follows one agreed layout: gameplay models/rules/services under `lib/src/core/gameplay/`, runtime config under `lib/src/core/config/`, JSON asset config under `assets/config/`, SQLite adapters under `lib/src/core/persistence/sqlite/`, and repositories at `lib/src/core/repositories/`. Keep the changes minimal and planning-focused; do not generate placeholder production code yet.</action>
  <verify>rg -n "core/gameplay|core/config|assets/config|core/persistence/sqlite|wallet_repository|session_checkpoint_repository|game_rules.json" .planning/PROJECT.md .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md</verify>
  <acceptance_criteria>
    - future folders are named explicitly before Phase 2 coding starts
    - the chosen layout is compatible with the repo's lightweight architecture goals
    - config asset and config loader locations are explicit
  </acceptance_criteria>
  <done>Persistence and gameplay code now have a planned home in the repo structure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "sqflite|shared_preferences|game_results|player_wallet|joker_inventory|session_checkpoint|word_crush.db|game_rules.json|JSON|YAML|config-driven|code-driven" .planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-DATA-ARCHITECTURE.md`
- [ ] `rg -n "core/gameplay|core/config|assets/config|core/persistence/sqlite|wallet_repository|session_checkpoint_repository|game_rules.json" .planning/PROJECT.md .planning/codebase/ARCHITECTURE.md .planning/codebase/STRUCTURE.md`
- [ ] Manual check: the chosen schema can restore grid, remaining moves, elapsed time and coin state after app resume
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Persistence and folder-structure decisions are frozen before gameplay implementation
  </success_criteria>

<output>
After completion, create `.planning/phases/01.1-requirements-architecture-and-persistence-eval-gate/01.1-02-SUMMARY.md`
</output>
