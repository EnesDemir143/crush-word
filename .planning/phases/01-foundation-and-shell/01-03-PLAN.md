---
phase: 01-foundation-and-shell
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - pubspec.yaml
  - assets/dictionary/tr_words.txt
  - lib/src/core/repositories/dictionary_repository.dart
  - lib/src/core/models/game_difficulty.dart
  - lib/src/core/models/game_config.dart
  - lib/src/core/models/game_result.dart
  - test/core/dictionary_repository_test.dart
autonomous: true
requirements:
  - PLAT-02
user_setup: []
must_haves:
  truths:
    - The application packages an internal Turkish dictionary asset for offline validation.
    - Shared gameplay models exist for difficulty, session config and result records.
  artifacts:
    - assets/dictionary/tr_words.txt
    - lib/src/core/repositories/dictionary_repository.dart
    - lib/src/core/models/game_config.dart
  key_links:
    - pubspec.yaml declares the dictionary asset used by dictionary_repository.dart.
---

<objective>
Add the offline dictionary pipeline and the shared domain models needed by later phases.

Purpose: Make gameplay implementation independent from external services and avoid re-defining core models ad hoc in later plans.
Output: Dictionary asset, repository loader, core configuration/result models and a smoke test.
</objective>

<execution_context>
@/Users/enesdemir/.codex/get-shit-done/workflows/execute-plan.md
@/Users/enesdemir/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-shell/01-CONTEXT.md
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package and normalize the Turkish dictionary</name>
  <files>pubspec.yaml, assets/dictionary/tr_words.txt, lib/src/core/repositories/dictionary_repository.dart</files>
  <read_first>.planning/research/SUMMARY.md, docs/Yazlab 2- Proje 2.pdf</read_first>
  <action>Add a packaged dictionary asset and a repository that loads it once, normalizes Turkish casing consistently and exposes efficient `contains(word)` / lookup helpers for later game logic. Keep the API intentionally small; do not build a backend adapter or sync layer.</action>
  <verify>flutter test test/core/dictionary_repository_test.dart</verify>
  <acceptance_criteria>
    - `pubspec.yaml` declares the dictionary asset
    - dictionary repository supports offline word lookup
  </acceptance_criteria>
  <done>Offline dictionary loading is in place and ready for validation logic.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared game configuration and result models</name>
  <files>lib/src/core/models/game_difficulty.dart, lib/src/core/models/game_config.dart, lib/src/core/models/game_result.dart</files>
  <read_first>.agent/rules/03-yeni-oyun-ve-zorluk.md, .agent/rules/07-skor-gecmisi-ve-oyun-bitisi.md</read_first>
  <action>Create the core models that represent difficulty/grid selection, move budget, final game results and the data needed later by score history. Keep these models serialization-friendly so the same shapes can be saved locally without inventing a second DTO layer.</action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - difficulty model encodes the exact 6x6/8x8/10x10 and move-count rules
    - result model carries the fields required by the score-history screen
  </acceptance_criteria>
  <done>Core gameplay models exist in a stable shared location for later phases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze`
- [ ] `flutter test test/core/dictionary_repository_test.dart`
- [ ] Dictionary asset loads without network dependence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared domain models are ready for gameplay phases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-shell/01-03-SUMMARY.md`
</output>
