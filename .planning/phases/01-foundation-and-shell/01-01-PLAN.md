---
phase: 01-foundation-and-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/src/app/word_crush_app.dart
  - lib/src/app/app_router.dart
  - lib/src/core/theme/app_theme.dart
  - test/app/app_bootstrap_test.dart
autonomous: true
requirements:
  - HOME-01
user_setup: []
must_haves:
  truths:
    - The starter counter app is removed and the app boots through a Word Crush shell.
    - The shell exposes named navigation targets for onboarding, home, new game, market and score history.
  artifacts:
    - lib/src/app/word_crush_app.dart
    - lib/src/app/app_router.dart
    - lib/src/core/theme/app_theme.dart
  key_links:
    - lib/main.dart wires the app shell instead of inline demo UI.
---

<objective>
Replace the default Flutter demo with the first real app shell for Word Crush.

Purpose: Establish a clean entry point and navigation backbone before feature work begins.
Output: App shell, theme, route definitions and a bootstrap test.
</objective>

<execution_context>
@/Users/enesdemir/.codex/get-shit-done/workflows/execute-plan.md
@/Users/enesdemir/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-shell/01-CONTEXT.md
@lib/main.dart
@pubspec.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the app shell and routing backbone</name>
  <files>lib/main.dart, lib/src/app/word_crush_app.dart, lib/src/app/app_router.dart</files>
  <read_first>.planning/phases/01-foundation-and-shell/01-CONTEXT.md, .agent/rules/02-giris-ve-ana-ekran.md, lib/main.dart</read_first>
  <action>Move the current inline demo widget tree out of `lib/main.dart` and replace it with a minimal bootstrap that starts `WordCrushApp`. Define named routes or an equivalent central navigation map for onboarding, home, new game, market and score history so later phases do not hardcode ad-hoc navigation in random widgets.</action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - `lib/main.dart` no longer contains the counter-page demo widgets
    - `lib/src/app/app_router.dart` defines navigation entries for all main screens
  </acceptance_criteria>
  <done>Starter shell is replaced by a real Word Crush app entry and centralized route map.</done>
</task>

<task type="auto">
  <name>Task 2: Add a reusable application theme and bootstrap test</name>
  <files>lib/src/core/theme/app_theme.dart, test/app/app_bootstrap_test.dart</files>
  <read_first>pubspec.yaml, analysis_options.yaml</read_first>
  <action>Create an app theme file that removes the default demo styling and gives the project a stable typography/color baseline without over-designing the UI yet. Add a small widget test that verifies the app boots and renders the first shell screen without referencing the deleted counter demo text.</action>
  <verify>flutter test test/app/app_bootstrap_test.dart</verify>
  <acceptance_criteria>
    - `test/app/app_bootstrap_test.dart` boots `WordCrushApp`
    - no test or source file still expects `Flutter Demo` copy
  </acceptance_criteria>
  <done>Theme and app bootstrap are testable and no starter-demo branding remains.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze`
- [ ] `flutter test test/app/app_bootstrap_test.dart`
- [ ] App shell shows Word Crush entry flow instead of counter demo
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Navigation backbone exists for the main product areas
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-shell/01-01-SUMMARY.md`
</output>
