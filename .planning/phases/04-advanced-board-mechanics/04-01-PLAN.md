---
phase: 04-advanced-board-mechanics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/core/gameplay/services/board_analyzer.dart
  - lib/src/core/gameplay/services/board_recovery.dart
  - lib/src/features/game/game_controller.dart
  - lib/src/features/game/widgets/game_header.dart
  - test/core/board_analyzer_test.dart
autonomous: true
requirements:
  - GRID-02
  - GRID-08
user_setup: []
must_haves:
  truths:
    - The game can compute a playable-word count for the current board.
    - Start and post-move boards recover automatically when no playable word remains.
    - The UI shows the current playable-word count.
  artifacts:
    - lib/src/core/gameplay/services/board_analyzer.dart
    - lib/src/core/gameplay/services/board_recovery.dart
    - lib/src/features/game/widgets/game_header.dart
  key_links:
    - game_controller.dart updates the header from board_analyzer.dart and uses board_recovery.dart when needed; this plan is the authoritative owner of GRID-02 dead-board recovery.
---

<objective>
Make the board analyzable and self-healing so the player is never stuck without a word.

Purpose: Close the biggest algorithmic risk in the project before layering combo and power behavior on top.
Output: Playable-word analyzer, recovery strategy, header integration and tests.
</objective>

<execution_context>
@/Users/enesdemir/.codex/get-shit-done/workflows/execute-plan.md
@/Users/enesdemir/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-board-mechanics/04-CONTEXT.md
@.planning/phases/03-core-gameplay-and-session-results/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement playable-word counting with non-overlap-aware search</name>
  <files>lib/src/core/gameplay/services/board_analyzer.dart, lib/src/features/game/game_controller.dart, lib/src/features/game/widgets/game_header.dart</files>
  <read_first>.agent/rules/06-grid-gecerlilik-ve-jokerler.md, lib/src/core/repositories/dictionary_repository.dart</read_first>
  <action>Build a board analyzer that searches the grid with adjacency rules and exposes at least one metric for the number of currently playable words, using a non-overlapping selection strategy for the displayed count. The expected implementation strategy is trie-backed DFS + backtracking with prefix pruning so invalid prefixes are cut early instead of exploring the full search space. Feed the result into the game header so the player can always see the current count after session creation and after each move.</action>
  <verify>flutter test test/core/board_analyzer_test.dart</verify>
  <acceptance_criteria>
    - analyzer uses the same adjacency rules as gameplay
    - analyzer implementation uses trie/prefix pruning rather than flat full-word brute-force search
    - game header renders the current playable-word count from controller state
  </acceptance_criteria>
  <done>The board can be inspected for playable words and the count is visible in UI.</done>
</task>

<task type="auto">
  <name>Task 2: Recover dead boards by shuffle then controlled regeneration</name>
  <files>lib/src/core/gameplay/services/board_recovery.dart, lib/src/features/game/game_controller.dart, test/core/board_analyzer_test.dart</files>
  <read_first>.planning/phases/04-advanced-board-mechanics/04-CONTEXT.md, .agent/rules/06-grid-gecerlilik-ve-jokerler.md</read_first>
  <action>Implement a recovery service that first tries shuffling existing letters and, if the analyzer still reports zero playable words, falls back to controlled regeneration. The controller should invoke this flow at session start and after any board-changing move, without leaving the player on an unsolved board. This plan closes the authoritative GRID-02 behavior rather than treating it as an optional enhancement.</action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - zero-playable boards trigger an automatic recovery path
    - recovery prefers shuffle before full regeneration
  </acceptance_criteria>
  <done>Boards no longer remain in an unwinnable state after creation or refill.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter test test/core/board_analyzer_test.dart`
- [ ] `flutter analyze`
- [ ] MANUAL GATE: on a crafted board, the visible playable-word count matches the documented non-overlapping expectation
- [ ] MANUAL GATE: a forced dead board is automatically recovered before the phase is closed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Board solvability and word-count visibility work together in the live session
  </success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-board-mechanics/04-01-SUMMARY.md`
</output>
