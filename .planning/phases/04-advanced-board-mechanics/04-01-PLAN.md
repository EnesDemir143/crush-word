---
phase: 04-advanced-board-mechanics
plan: 01
type: execute
wave: 1
depends_on:
  - "02.1-01"
  - "03-02"
files_modified:
  - lib/src/core/gameplay/services/board_analyzer.dart
  - lib/src/core/gameplay/services/board_recovery.dart
  - lib/src/features/game/game_controller.dart
  - lib/src/features/game/widgets/game_header.dart
  - test/core/board_analyzer_test.dart
autonomous: true
requirements:
  - GRID-02
  - GRID-08
user_setup: []
must_haves:
  truths:
    - The game can compute a playable-word count for the current board.
    - Post-move boards recover automatically when no playable word remains.
    - The UI shows the current playable-word count.
  artifacts:
    - lib/src/core/gameplay/services/board_analyzer.dart
    - lib/src/core/gameplay/services/board_recovery.dart
    - lib/src/features/game/widgets/game_header.dart
  key_links:
    - game_controller.dart updates the header from board_analyzer.dart and uses board_recovery.dart after board-changing moves; this plan extends the initial guard from `02.1-01` into post-move continuity and visible count ownership.
---

<objective>
Extend the initial solvability guard into live gameplay so board-changing moves keep the game visible and analyzable.

Purpose: Build on the earlier first-render protection by adding visible playable-word count and post-move self-healing before combo and power mechanics layer on top.
Output: Extended playable-word analyzer, post-move recovery strategy, header integration and tests.
</objective>

<execution_context>
@/Users/enesdemir/.codex/get-shit-done/workflows/execute-plan.md
@/Users/enesdemir/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-board-mechanics/04-CONTEXT.md
@.planning/phases/03-core-gameplay-and-session-results/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement playable-word counting with non-overlap-aware search</name>
  <files>lib/src/core/gameplay/services/board_analyzer.dart, lib/src/features/game/game_controller.dart, lib/src/features/game/widgets/game_header.dart</files>
  <read_first>.agent/rules/06-grid-gecerlilik-ve-jokerler.md, lib/src/core/repositories/dictionary_repository.dart</read_first>
  <action>Extend the analyzer baseline introduced in Phase `02.1-01` so it not only finds the first playable word, but can also compute a non-overlapping playable-word count for the live session header. Keep the trie-backed DFS + backtracking with prefix pruning strategy, and make sure the counting mode reuses the same adjacency/no-repeat rules rather than creating a second rule set.</action>
  <verify>flutter test test/core/board_analyzer_test.dart</verify>
  <acceptance_criteria>
    - analyzer uses the same adjacency rules as gameplay
    - analyzer implementation uses trie/prefix pruning rather than flat full-word brute-force search
    - game header renders the current playable-word count from controller state
  </acceptance_criteria>
  <done>The board can be inspected for playable words and the count is visible in UI.</done>
</task>

<task type="auto">
  <name>Task 2: Recover post-move dead boards by shuffle then controlled regeneration</name>
  <files>lib/src/core/gameplay/services/board_recovery.dart, lib/src/features/game/game_controller.dart, test/core/board_analyzer_test.dart</files>
  <read_first>.planning/phases/04-advanced-board-mechanics/04-CONTEXT.md, .agent/rules/06-grid-gecerlilik-ve-jokerler.md</read_first>
  <action>Implement the post-move branch of recovery so that after any board-changing resolution/refill, the controller rechecks playability and heals the board if needed. Recovery should still prefer shuffle before controlled regeneration, but this plan no longer owns the first-render case from Phase `02.1-01`; it owns continuity after live board mutations and keeps the visible session from drifting into a dead state.</action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - zero-playable post-move boards trigger an automatic recovery path
    - recovery prefers shuffle before full regeneration
  </acceptance_criteria>
  <done>Boards no longer remain in an unwinnable state after live move resolution or refill.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter test test/core/board_analyzer_test.dart`
- [ ] `flutter analyze`
- [ ] MANUAL GATE: on a crafted board, the visible playable-word count matches the documented non-overlapping expectation
- [ ] MANUAL GATE: a forced post-move dead board is automatically recovered before the phase is closed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Post-move board continuity and word-count visibility work together in the live session
  </success_criteria>

<output>
After completion, create `.planning/phases/04-advanced-board-mechanics/04-01-SUMMARY.md`
</output>
