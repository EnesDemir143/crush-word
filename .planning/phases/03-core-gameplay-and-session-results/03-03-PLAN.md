---
phase: 03-core-gameplay-and-session-results
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - pubspec.yaml
  - lib/src/core/persistence/sqlite/app_database.dart
  - lib/src/core/repositories/game_history_repository.dart
  - lib/src/core/repositories/session_checkpoint_repository.dart
  - lib/src/features/game/game_controller.dart
  - lib/src/features/game/game_screen.dart
  - lib/src/features/game/exit_confirmation_dialog.dart
  - test/features/game/endgame_flow_test.dart
autonomous: true
requirements:
  - END-01
  - END-02
user_setup: []
must_haves:
  truths:
    - When moves reach zero, the result is saved and the user returns to home.
    - Back-exit asks for Evet/Hayir and preserves or saves the session accordingly.
  artifacts:
    - pubspec.yaml
    - lib/src/core/persistence/sqlite/app_database.dart
    - lib/src/core/repositories/game_history_repository.dart
    - lib/src/core/repositories/session_checkpoint_repository.dart
    - lib/src/features/game/exit_confirmation_dialog.dart
    - lib/src/features/game/game_controller.dart
  key_links:
    - game_controller.dart writes completed results through a sqflite-backed game_history_repository.dart before navigation away.
---

<objective>
Persist finished sessions and implement the required exit/endgame flows.

Purpose: Close the core game loop so every completed or confirmed-aborted session becomes usable score-history data while freezing the baseline checkpoint storage needed for future resume.
Output: SQLite bootstrap, game history repository, session checkpoint baseline, endgame save flow, exit confirmation dialog and tests.
</objective>

<execution_context>
@/Users/enesdemir/.codex/get-shit-done/workflows/execute-plan.md
@/Users/enesdemir/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-gameplay-and-session-results/03-CONTEXT.md
@.planning/phases/03-core-gameplay-and-session-results/03-02-SUMMARY.md
@.agent/rules/07-skor-gecmisi-ve-oyun-bitisi.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Save completed results and baseline checkpoints through dedicated repositories</name>
  <files>pubspec.yaml, lib/src/core/persistence/sqlite/app_database.dart, lib/src/core/repositories/game_history_repository.dart, lib/src/core/repositories/session_checkpoint_repository.dart, lib/src/features/game/game_controller.dart</files>
  <read_first>lib/src/core/models/game_result.dart, .agent/rules/07-skor-gecmisi-ve-oyun-bitisi.md</read_first>
  <action>Add `sqflite` as the structured local persistence dependency, open a small app database under `lib/src/core/persistence/sqlite/` and create the `game_results` table plus the baseline `session_checkpoint` table defined by the data architecture. Back the history repository with that SQLite layer so finished sessions are inserted once and queried newest-first. Add `session_checkpoint_repository.dart` as the canonical save/load boundary for active-session snapshots even if full app-resume UI wiring lands later. Integrate the history path into the controller so move depletion creates a final result record exactly once before routing back to home, while leaving `shared_preferences` limited to profile/settings concerns.</action>
  <verify>flutter test test/features/game/endgame_flow_test.dart</verify>
  <acceptance_criteria>
    - a local `game_results` table exists and stores the fields needed by the future score-history screen
    - a baseline `session_checkpoint` repository and table exist for future resume wiring
    - move depletion persists one result record and returns to home
    - saved results can be read newest-first through the sqflite-backed repository
  </acceptance_criteria>
  <done>Finished sessions and baseline checkpoint storage are persisted in a way later phases can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the confirmed back-exit flow</name>
  <files>lib/src/features/game/game_screen.dart, lib/src/features/game/exit_confirmation_dialog.dart, lib/src/features/game/game_controller.dart, test/features/game/endgame_flow_test.dart</files>
  <read_first>.planning/phases/03-core-gameplay-and-session-results/03-CONTEXT.md, .agent/rules/07-skor-gecmisi-ve-oyun-bitisi.md</read_first>
  <action>Add a two-option confirmation dialog for exiting mid-game and wire it so `Hayir` resumes the active session with no state loss while `Evet` saves the current result snapshot and returns to home. Reuse the same sqflite-backed repository path as end-of-moves completion instead of inventing a second save flow.</action>
  <verify>flutter analyze</verify>
  <acceptance_criteria>
    - back-exit displays Evet/Hayir choices
    - Hayir resumes play, Evet saves and exits through the same result pipeline
  </acceptance_criteria>
  <done>Both required game-ending paths are implemented consistently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `flutter analyze`
- [ ] `flutter test test/features/game/endgame_flow_test.dart`
- [ ] MANUAL GATE: hamle sifira indiginde ve onayli cikista sonuc kaydi yapilip ana ekrana donulur; kullanici onayi olmadan plan kapanmaz
- [ ] If this plan also exposes app-resume behavior: MANUAL GATE for background -> reopen -> state restore; otherwise defer that manual gate to the first resume-enabled plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Core gameplay loop now closes with persisted session results
  </success_criteria>

<output>
After completion, create `.planning/phases/03-core-gameplay-and-session-results/03-03-SUMMARY.md`
</output>
