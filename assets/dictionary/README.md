This folder contains the packaged offline dictionary asset used by Word Crush.

- `tr_words.txt` is the normalized word list loaded by the Flutter app.
- The list was generated from a locally downloaded copy of `tdd-ai/hunspell-tr`.
- The raw import folder is intentionally git-ignored so only the runtime asset is tracked.
- Upstream license: MPL 2.0.
